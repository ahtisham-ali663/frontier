export const PADDING = 16
export const MAX_WIDTH = 1200
export const MAX_WIDTH_WITH_PADDING = MAX_WIDTH + 2 * PADDING

export const COMPONENT_WRAPPER = {
  maxWidth: MAX_WIDTH_WITH_PADDING,
  padding: `0px ${PADDING}px`,
  margin: 'auto',
}
